ANTHROPIC_API_KEY=
# Optional: override default model (claude-sonnet-4-6)
ANTHROPIC_MODEL=

# Threads API (https://developers.facebook.com/docs/threads) — ⑥自動投稿用
THREADS_ACCESS_TOKEN=
THREADS_USER_ID=

# /api/cron/publish-scheduled を保護するための共有シークレット(任意)
# Vercel Cron / 外部cronから Authorization: Bearer <値> で呼び出す
CRON_SECRET=
